# Trading Dashboard 프로젝트 작업 지침

## 작업 원칙

- 기존 프로젝트 구조와 코딩 스타일을 존중한다.
- 요구사항과 직접 관련된 최소한의 파일만 변경한다.
- 민감정보, 운영 데이터, 기존 사용자의 작업을 보존한다.
- 작업을 완료하기 전에 관련 검증을 수행한다.

## 테스트 실행 환경

- 모든 테스트는 프로젝트 가상환경 `.venv`의 Python으로 실행한다.
- 전체 테스트의 표준 명령은 `.venv/bin/python -m unittest discover -s tests`이다.
- 가상환경을 활성화한 경우에는 `python -m unittest discover -s tests`를 사용할 수 있다.
- 시스템 Python(`python3`)을 직접 호출하지 말고, 테스트 전에 `sys.executable`이 프로젝트의 `.venv/bin/python`인지 확인한다.

## 작업 완료 후 반영 규칙

변경 작업이 끝났고 검증 결과 문제가 없으며 사용자가 별도로 중단하거나
다른 반영 방식을 지정하지 않았다면, 다음 순서로 변경사항을 반영한다.

1. 의도하지 않은 변경과 민감정보가 없는지 확인하고 관련 테스트 및 구성을 검증한다.
2. 이번 작업에 포함되는 파일만 스테이징해 하나의 목적을 가진 커밋을 만든다.
3. 현재 작업 브랜치의 원격 저장소에 푸시한다.
4. 배포 호스트의 프로젝트 디렉터리에서 Docker Compose 서비스를 재빌드·재기동한다.
5. 컨테이너 상태, health endpoint, 변경된 기능을 확인한다.

상세한 명령과 중단 조건은 [`docs/work-completion-policy.md`](docs/work-completion-policy.md)를
기준으로 한다. 테스트 실패, 변경 범위 불명확, 비밀값 노출 가능성, 원격 저장소
또는 배포 호스트 접근 불가 등 문제가 하나라도 있으면 커밋·푸시·배포를 진행하지
말고 원인과 필요한 조치를 보고한다.

기존에 작업 중이던 변경사항이 있으면 이를 임의로 되돌리거나 함께 커밋하지 않는다.
`git status`와 diff를 먼저 확인하고 이번 작업에 해당하는 파일만 명시적으로
스테이징한다.
